# Third-party notices

Local Converter uses the following third-party components:

- **ImageMagick / Magick.NET** — ImageMagick License, available from <https://imagemagick.org/script/license.php>.
- **FFmpeg** — the build placed in the installation package is downloaded from BtbN/FFmpeg-Builds during the build. Its exact license file is copied next to `ffmpeg.exe`. The default shared build is distributed under GPLv3; see <https://ffmpeg.org/legal.html>.
- **Zig toolchain** — used only to build the native shell extension and is not included in the installed application.

No user file is sent to any third party by Local Converter.
